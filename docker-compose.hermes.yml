services:
  app:
    image: infinite-canvas:jonoka-local
    build:
      context: .
      dockerfile: Dockerfile
    container_name: infinite-canvas
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:3010:3000"
    restart: unless-stopped
